en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            password:
              too_short: must be atleast 8 characters long
            email:
              invalid: must be a valid email address 
